# template_version: 3.0.0
# status: stub
# roadmap_status: v3.1-planned
# filled_by: Batch P4 (ADR-008); v3.1 release

name: branch-name-check (stub)

on:
  workflow_dispatch:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - name: Stub
        run: echo "PR-7 (TASK-0007) will author this workflow"
