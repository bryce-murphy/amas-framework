# template_version: 3.0.0
# status: stub
# filled_by: per ADR-003

name: ai-session-log-check (stub)

on:
  workflow_dispatch:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - name: Stub
        run: echo "PR-7 (TASK-0007) will author this workflow"
